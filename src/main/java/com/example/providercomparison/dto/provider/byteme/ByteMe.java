package com.example.providercomparison.dto.provider.byteme;

public class ByteMe {
    /*
     * Protocol: CSV over HTTP
     * Auth: X-Api-Key
     * Product shape: Flat CSV row
     * Voucher: absolute / percentage indicated by two columns
     * Duplicates: yes (same row repeated)
     * Paging: query only
     */
}
